#include <iostream> //Includes the standard input output stream library.

using namespace std; //Allows us to avoid writing std:: before standard functions.

int main() //The main function where the program begins execution. () is a mnemonic used to seperate functions from line isweird
{

cout<<"Hello, World!"; // Outputs text to screen.

    return 0; //Ends the program successfully
}


/*(//) is used to write a single comment, something which doesnt affect the code at all and is just for the programmer's sake. 
  (/*) is used to write multiple comments.*/